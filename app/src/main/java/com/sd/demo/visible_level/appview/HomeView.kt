package com.sd.demo.visible_level.appview;

import android.content.Context;

import androidx.annotation.NonNull;

public class HomeView extends BaseAppView
{
    public HomeView(@NonNull Context context)
    {
        super(context);
    }
}
